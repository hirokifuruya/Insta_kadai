<h1><%= @user.name %>のページ</h1>

<p>メールアドレス: <%= @user.email %></p>
<p>
  <strong>Icon:</strong>
  <%= if @user.image.present?
        image_tag @user.image.url, size: "40x40"
      else
      image_tag "bird_img", size: "40x40"
      end
  %>
</p>
